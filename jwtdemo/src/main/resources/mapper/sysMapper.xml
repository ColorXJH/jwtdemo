<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="jwtdemo.dao.SysDao">
	<select id="getPasswordFromDB" resultType="java.lang.String">
		select password from scott.db_user where username=#{username}
	</select>
</mapper>